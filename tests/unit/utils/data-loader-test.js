import dataLoader from 'shelter-art-foundation/utils/data-loader';
import { module, test } from 'qunit';

module('Unit | Utility | data-loader', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dataLoader();
    assert.ok(result);
  });
});
